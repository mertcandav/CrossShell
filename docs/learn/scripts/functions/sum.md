# Function: sum

Returns the total of all parameters.

Overloadings:
+ ``avg(<params>)``

Example use:
```
var name = sum(100,100,50,10);
print(name);
delete name;

>CDBASEPATH

#>
OUTPUT

260
<#
```
